The package ffmpeg provides CMake targets:

    find_package(FFMPEG CONFIG REQUIRED)
    target_link_libraries(main PRIVATE
        FFMPEG::avcodec
        FFMPEG::avformat
        FFMPEG::avutil
        FFMPEG::swresample
        FFMPEG::swscale
    )
